[package]
name = "rusty_link"
version = "0.3.6"
authors = ["Andreas Mueller <anzbert@gmail.com>"]
edition = "2021"
description = "Rust bindings for Ableton Link's C Wrapper Extension (abl_link)"
repository = "https://github.com/anzbert/rusty_link"
readme = "README.md"
license = "GPL-2.0-or-later"
keywords = ["ableton", "link"]
categories = ["api-bindings"]

[dependencies]
# None

[dev-dependencies]
crossterm = "^0.25.0"
cpal = "~0.14.2"
# cpal = { version = "~0.14.2", features = ["asio"] }
# cpal = { version = "~0.14.2", features = ["jack"] }

[build-dependencies]
cmake = "^0.1.49"
bindgen = "^0.63.0"
