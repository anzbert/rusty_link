[package]
name = "rusty_link"
version = "0.2.0"
authors = ["Andreas Mueller <anzbert@gmail.com>"]
edition = "2021"
description = "Plain Rust bindings for Ableton Link's C 11 Wrapper"
repository = "https://github.com/anzbert/rusty_link"
readme = "README.md"
license-file = "LICENSE"
keywords = ["ableton", "link"]
categories = ["api-bindings"]


[dependencies]
# This crate is using forked code from ffi_helpers, which adds fn split_closure_trailing_data() 
# and  a corresponding trait. Pull request pending.
# ffi_helpers = "^0.3.0"
# ffi_helpers = { git = "https://github.com/anzbert/ffi_helpers", version = "0.3.0" }

[dev-dependencies]
ctrlc = "^3.2.3" # for link_hut example

[build-dependencies]
cmake = "^0.1.48"
bindgen = "^0.60.1"
